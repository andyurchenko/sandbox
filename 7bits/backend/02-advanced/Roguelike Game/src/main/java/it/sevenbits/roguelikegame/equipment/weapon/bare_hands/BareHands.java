package it.sevenbits.roguelikegame.equipment.weapon.bare_hands;

import it.sevenbits.roguelikegame.equipment.weapon.Weapon;


/**
 * The type Bare hands.
 */
public class BareHands extends Weapon {

    /**
     * Instantiates a new Bare hands.
     *
     * @param name   the name
     * @param size   the size
     * @param weight the weight
     * @param damage the damage
     */
    public BareHands(final String name, final int size, final int weight, final int damage) {
        super(name, size, weight, damage);
    }
}
